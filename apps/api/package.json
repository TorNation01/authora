{
  "name": "authora-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd ../.. && PYTHONPATH=apps/api python -m uvicorn authora.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Python API - no build step'",
    "start": "cd ../.. && PYTHONPATH=apps/api python -m uvicorn authora.main:app --host 0.0.0.0 --port 8000",
    "test": "python -m pytest tests -v --tb=short"
  }
}
