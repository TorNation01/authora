[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "authora-worker"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "redis>=5.0.0",
]
